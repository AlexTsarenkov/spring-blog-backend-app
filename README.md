# Back-end приложения для блога в рамках курса Яндекс.Практикум (Спринт 3)

Бэкенд веб-приложения блога, реализованный на **Spring Framework 6.1+** без использования Spring Boot.  
Приложение предоставляет REST API для управления постами, комментариями, лайками и изображениями.

---

## Технологический стек

- **Java:** 21  
- **Spring Framework:** 6.1+ (без Spring Boot)  
- **Сервлет-контейнер:** Tomcat / Jetty  
- **Сборка:** Maven 
- **База данных:**  
  - In-memory (H2) **или**
  - Персистентная (PostgreSQL)
- **Тестирование:**  
  - JUnit 5  
  - Spring TestContext Framework  

## Запуск приложения

После сборки и деплоя в сервлет-контейнер приложение доступно по адресу http://localhost:8080


## Эндпоинты

### Получение списка постов

GET /api/posts

#### Пример запроса

GET /api/posts?search=Lalala&pageNumber=1&pageSize=5

#### Ответ 200 OK

```json
{
  "posts": [
    {
      "id": 1,
      "title": "Название поста 1",
      "text": "Текст поста в формате Markdown...",
      "tags": ["tag_1", "tag_2"],
      "likesCount": 5,
      "commentsCount": 1
    }
  ],
  "hasPrev": true,
  "hasNext": false,
  "lastPage": 3
}
```
---

### Получение поста

POST /api/posts/{id}

#### Path parameters

| Параметр | Тип    | Описание |
|---------|--------|----------|
| id      | number | Идентификатор поста |

#### Ответ 200 OK

```json
{
  "id": 1,
  "title": "Название поста 1",
  "text": "Полный текст поста в формате Markdown...",
  "tags": ["tag_1", "tag_2"],
  "likesCount": 5,
  "commentsCount": 1
}
```

---

### Добавление поста

POST /api/posts

#### Тело запроса

```json
{
  "title": "Название поста 3",
  "text": "Текст поста в формате Markdown...",
  "tags": ["tag_1", "tag_2"]
}
```

#### Ответ 200 OK

```json
{
  "id": 3,
  "title": "Название поста 3",
  "text": "Текст поста в формате Markdown...",
  "tags": ["tag_1", "tag_2"],
  "likesCount": 0,
  "commentsCount": 0
}
```

---

### Редактирование поста

PUT /api/posts/{id}

#### Тело запроса

```json
{
  "id": 3,
  "title": "Название поста 3",
  "text": "Обновлённый текст поста...",
  "tags": ["tag_1", "tag_2"]
}
```

---

### Удаление поста

DELETE /api/posts/{id}

Ответ:
- 200 OK
- Пост и все комментарии удаляются

---

### Лайк поста

POST /api/posts/{id}/likes

#### Ответ 200 OK

```json
6
```

---

### Обновление изображения поста

PUT /api/posts/{id}/image

Content-Type: multipart/form-data

Поле формы:
- image — файл изображения

Ответ: 200 OK

---

### Получение изображения поста

GET /api/posts/{id}/image

Ответ:
- массив байт изображения

---

## 💬 Комментарии

### Получение комментариев поста

GET /api/posts/{postId}/comments

#### Ответ 200 OK

```json
[
  {
    "id": 1,
    "text": "Комментарий к посту 1",
    "postId": 1
  },
  {
    "id": 2,
    "text": "Ещё один комментарий к посту 1",
    "postId": 1
  }
]
```

---

### Получение комментария

GET /api/posts/{postId}/comments/{commentId}

#### Ответ 200 OK

```json
{
  "id": 2,
  "text": "Ещё один комментарий к посту 1",
  "postId": 1
}
```

---

### Добавление комментария

POST /api/posts/{postId}/comments

#### Тело запроса

```json
{
  "text": "Комментарий к посту",
  "postId": 1
}
```

#### Ответ 200 OK

```json
{
  "id": 2,
  "text": "Комментарий к посту",
  "postId": 1
}
```

---

### Редактирование комментария

PUT /api/posts/{postId}/comments/{commentId}

#### Тело запроса

```json
{
  "id": 2,
  "text": "Второй комментарий к посту 1",
  "postId": 1
}
```

---

### Удаление комментария

DELETE /api/posts/{postId}/comments/{commentId}

Ответ: 200 OK

---

## Тестирование

Проект покрыт юнит- и интеграционными тестами с использованием:
- JUnit 5
- Spring TestContext Framework

---

## Сборка проекта
### Очистка и сборка
mvn clean package

### Запуск тестов
mvn test

